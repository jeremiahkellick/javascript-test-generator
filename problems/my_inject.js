// Monkey patch the Array class and add a myReduce method. If myReduce receives
// no argument, then use the first element of the array as the default
// accumulator.

Array.prototype.myReduce = function(func, acc) {

};
